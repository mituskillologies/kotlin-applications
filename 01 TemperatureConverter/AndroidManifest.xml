<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- The application tag contains all global settings for the app -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TemperatureConverterApp"
        tools:targetApi="31"
        >


        <!-- 1. Declare ResultActivity -->
        <activity
            android:name=".ResultActivity"
            android:exported="false"
            android:label="Result"
            android:parentActivityName=".ConverterActivity"/>

        <!-- 2. Declare ConverterActivity -->
        <activity
            android:name=".ConverterActivity"
            android:exported="true"
            android:label="Convert">

            <!-- Set ConverterActivity as the launcher activity (Task Requirement) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
